!function(t, a, e, i) {
    var n = function(a, e) {
        this.ele = a,
        this.defaults = {
            currentPage: 1,
            totalPage: 10,
            isShow: !0,
            count: 5,
            homePageText: "首页",
            endPageText: "尾页",
            prevPageText: "上一页",
            nextPageText: "下一页",
            callback: function() {}
        },
        this.opts = t.extend({},
        this.defaults, e),
        this.current = this.opts.currentPage,
        this.init()
    };
    n.prototype = {
        init: function() {
            this.isRender = !0,
            this.render(),
            this.eventBind()
        },
        render: function() {
            var t = this.opts,
            a = this.current,
            e = t.totalPage,
            i = this.getPagesTpl();
            this.homePage = '<li><a href="#" class="ui-pagination-page-item" data-current="1">' + t.homePageText + "</a></li>",
            this.prevPage = '<li><a href="#" class="ui-pagination-page-item" data-current="' + (a - 1) + '">' + t.prevPageText + "</a></li>",
            this.nextPage = '<li><a href="#" class="ui-pagination-page-item" data-current="' + (a + 1) + '">' + t.nextPageText + "</a></li>",
            this.endPage = '<li><a href="#" class="ui-pagination-page-item" data-current="' + e + '">' + t.endPageText + "</a></li>",
            this.checkPage(),
            this.isRender && this.ele.html('<ul class="pagination pagination-split">' + this.homePage + this.prevPage + i + this.nextPage + this.endPage + "</ul>")
        },
        checkPage: function() {
            var t = this.opts,
            a = t.totalPage,
            e = this.current;
            t.isShow || (this.homePage = '<li class="disabled"><a href="#" class="ui-pagination-page-item" data-current="1">' + t.homePageText + "</a></li>",this.endPage = '<li class="disabled"><a href="#" class="ui-pagination-page-item" data-current="' + e + '">' + t.endPageText + "</a></li>"),
            1 === e && (this.homePage = '<li class="disabled"><a href="#" class="ui-pagination-page-item" data-current="1">' + t.homePageText + "</a></li>", this.prevPage ='<li class="disabled"><a href="#" class="ui-pagination-page-item" data-current="' + e + '">' + t.prevPageText + "</a></li>"),
            e === a && (this.endPage = '<li class="disabled"><a href="#" class="ui-pagination-page-item" data-current="' + e + '">' + t.endPageText + "</a></li>",this.nextPage = '<li class="disabled"><a href="#" class="ui-pagination-page-item" data-current="' + a + '">' + t.nextPageText + "</a></li>"),
            1 === a && (this.homePage = '<li class="disabled"><a href="#" class="ui-pagination-page-item" data-current="1">' + t.homePageText + "</a></li>",this.prevPage = '<li class="disabled"><a href="#" class="ui-pagination-page-item" data-current="' + e + '">' + t.prevPageText + "</a></li>",this.endPage = '<li class="disabled"><a href="#" class="ui-pagination-page-item" data-current="' + e + '">' + t.endPageText + "</a></li>", this.nextPage = '<li class="disabled"><a href="#" class="ui-pagination-page-item" data-current="' + a + '">' + t.nextPageText + "</a></li>"),
            a < 1 && (this.isRender = !1)
        },
        getPagesTpl: function() {
            var t = this.opts,
            a = t.totalPage,
            e = this.current,
            i = "",
            n = t.count;
            if (a <= n) 
            	for (g = 1; g <= a; g++) 
            		i += g === e ? '<li class="active"><a href="#" class="ui-pagination-page-item" data-current="' + g + '">' + g + "</a></li>": '<li><a href="#" class="ui-pagination-page-item" data-current="' + g + '">' + g + "</a></li>";
            else {
                var s = n / 2;
                if (e <= s) for (g = 1; g <= n; g++) i += g === e ? '<li class="active"><a href="#" class="ui-pagination-page-item" data-current="' + g + '">' + g + "</a></li>": '<li><a href="#" class="ui-pagination-page-item" data-current="' + g + '">' + g + "</a></li>";
                else {
                    var r = Math.floor(s),
                    c = e + r,
                    h = e - r,
                    o = n % 2 == 0;
                    c > a && (o ? (h -= c - a - 1, c = a + 1) : (h -= c - a, c = a)),
                    o || c++;
                    for (var g = h; g < c; g++) i += g === e ? '<li class="active"><a href="#" class="ui-pagination-page-item" data-current="' + g + '">' + g + "</a></li>": '<li><a href="#" class="ui-pagination-page-item" data-current="' + g + '">' + g + "</a></li>"
                }
            }
            return i
        },
        eventBind: function() {
            var a = this,
            e = this.opts.callback;
            this.ele.on("click", ".ui-pagination-page-item",
            function() {
                var i = t(this).data("current");
                a.current != i && (a.current = i, a.render(), e && "function" == typeof e && e(i))
            })
        }
    },
    t.fn.pagination = function(t) {
        return new n(this, t),
        this
    }
} (jQuery, window, document);